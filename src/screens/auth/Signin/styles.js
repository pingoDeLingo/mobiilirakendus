import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 12,
        height: '100%',
        flexDirection: 'column',
        borderWidth: 1,
    },
    button: {
        marginVertical: 16,
    },
    footerText: {
        textAlign: 'center',
        marginBottom: 56,
    },
    footerLink: {
        fontWeight: 'bold',
    }
});